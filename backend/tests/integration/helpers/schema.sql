-- DEPRECATED: integration/e2e tests now apply schema via the production migrator
-- (src/infrastructure/database/migrator.js + migrations/*.sql).
-- Kept only so older docs/scripts that reference this path are not confusingly empty.
SELECT 1;
