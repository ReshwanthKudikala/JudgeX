/**
 * @deprecated Use `ProblemCodeEditor` from `@/features/editor`.
 * Kept so the Sprint 22 slot name remains discoverable.
 */
export { ProblemCodeEditor as EditorPlaceholder } from '@/features/editor/components/ProblemCodeEditor';
